import React from "react";

import CartContainer from "../components/cart";

// ----------------------------------------------------------------------

const Cart = () => {
  return (
    <CartContainer />
  );
};

export default Cart;
